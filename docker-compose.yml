services:
  frontend:
    build:
      context: .
    container_name: ecommerce-frontend
    ports:
      - "3000:3000"
    restart: always
    networks:
      - ecommerce-net

networks:
  ecommerce-net:
    name: ecommerce-net
    external: true
